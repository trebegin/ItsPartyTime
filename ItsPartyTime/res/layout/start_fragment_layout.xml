<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/start_fragment_background"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin">
    <FrameLayout 
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/test"></FrameLayout>
    
    <Button 
        style="@style/ButtonTheme"
        android:layout_alignParentBottom="true"
        android:text="@string/join_party_button_text"
        android:id="@+id/join_party_button"/>
    
    <Button 
        style="@style/ButtonTheme"
        android:layout_above="@id/join_party_button"
        android:text="@string/create_party_button_text"
        android:id="@+id/create_party_button"/>
    
    <Button 
        style="@style/ButtonTheme"
        android:layout_above="@id/create_party_button"
        android:text="@string/login_button_text"
        android:id="@+id/login_button"/>

</RelativeLayout>
